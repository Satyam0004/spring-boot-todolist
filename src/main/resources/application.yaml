spring:
  application:
    name: store
  datasource:
    url: jdbc:mysql://localhost:3306/to_do_list?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: Satyam@2005
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
